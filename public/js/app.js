// Typejs - animate

$('animate').typed({
    strings: ["web developer", "UI/UX Designer", "Photography"],
    loop: true,
    startDelay: 1000,
    backDelay: 1000,
    typeSpeed: 30,
    showCursor: true,
    cursorChar: '|',
    autoInsertCss: true
});
